enum DomainFailureCode {
    EntityNotFound
    EntityExists
    AssertionFailed
    Unexpected
    AccessDenied
}

data DomainFailure {
    code: DomainFailureCode
    msg: opt[str]
}

data SuccessResponse {
    message: opt[str]
}